<% @appointments.each do |app| %>
    <%= link_to app.doctor.name, doctor_path(app.doctor_id) %>
    <%= link_to app.patient.name, patient_path(app.patient_id) %>
    <%= link_to app.appointment_datetime.strftime("%B %e, %Y %k:%M"), appointment_path(app) %>
<% end %>